{
  "config": {
    "step": {
      "user": {
        "title": "OpenClaw Setup",
        "description": "Connect to your OpenClaw server.",
        "data": {
          "url": "OpenClaw Server URL",
          "api_key": "API Key",
          "agent_id": "Agent ID (default: main)",
          "session_key": "Session Key (optional)",
          "timeout": "Request Timeout (seconds)",
          "verify_ssl": "Verify SSL Certificates"
        }
      },
      "confirm": {
        "title": "Confirm Configuration",
        "description": "**URL**: {url}\n**Agent ID**: {agent_id}"
      }
    },
    "error": {
      "url_required": "URL is required",
      "url_invalid_protocol": "URL must start with http:// or https://",
      "url_invalid_format": "Invalid URL format",
      "invalid_api_key": "Invalid API key (401 Unauthorized)",
      "connection_timeout": "Connection timeout — check if OpenClaw is running",
      "connection_refused": "Connection refused — check URL and port",
      "ssl_error": "SSL certificate error — try disabling SSL verification",
      "connection_unknown_error": "Unknown connection error — check server status"
    },
    "abort": {
      "already_configured": "Service is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "OpenClaw Options",
        "data": {
          "timeout": "Request Timeout (seconds)",
          "verify_ssl": "Verify SSL Certificates"
        }
      }
    }
  }
}
